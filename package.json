{
  "name": "bookmyevent",
  "version": "1.0.0",
  "description": "BookMyEvent - Interactive 3D Event Discovery & Reservation Full-Stack Platform",
  "main": "backend/src/index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npx --yes concurrently -n \"SERVER,CLIENT\" -c \"cyan.bold,yellow.bold\" \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "start": "npx --yes concurrently -n \"SERVER,CLIENT\" -c \"cyan.bold,yellow.bold\" \"npm start --prefix backend\" \"npm start --prefix client\"",
    "dev:server": "npm run dev --prefix backend",
    "dev:client": "npm run dev --prefix client",
    "build:client": "npm run build --prefix client",
    "seed": "npm run seed --prefix backend",
    "install:all": "npm install --prefix backend && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
